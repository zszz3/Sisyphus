using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Enemy_13 : Enemy
{
    // Start is called before the first frame update
    new public void Start()
    {
        base.Start();
    }

    new public void FiexedUpdate()
    {
        base.FixedUpdate();
    }
}
